<UserControl x:Class="WPFApp.PlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="50">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Image Width="28" Height="28"
               Source="{Binding PlayerImage}"
               Stretch="UniformToFill" />
        <TextBlock Text="{Binding ShirtNumber}" FontWeight="Bold" FontSize="8" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding PlayerName}" FontSize="6" HorizontalAlignment="Center" TextWrapping="Wrap" Width="50"/>
    </StackPanel>
</UserControl> 