<UserControl x:Class="WPFApp.PlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="60">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Image Width="32" Height="32" Source="{Binding PlayerImage}" Stretch="UniformToFill" />
        <TextBlock Text="{Binding ShirtNumber}" FontWeight="Bold" FontSize="11" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding Name}" FontSize="9" HorizontalAlignment="Center" TextWrapping="Wrap" Width="60"/>
    </StackPanel>
</UserControl> 